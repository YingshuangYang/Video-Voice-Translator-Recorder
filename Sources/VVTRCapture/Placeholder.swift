import Foundation

// Kept for now so the module always has at least one symbol even if
// capture code is conditionally compiled on older macOS versions.
public enum VVTRCapturePlaceholder {
  public static let note = "VVTRCapture module."
}

